<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All progress photos
    </h1>

    <a href="/progress_photos/new" class="btn btn-block btn-outline-secondary">
      Add a new progress photo
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Comment
        </th>

        <th>
          Picture
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @progress_photos.each do |progress_photo| %>
      <tr>
        <!-- Display progress_photo.id start -->
        <td>
          <%= progress_photo.id %>
        </td>
        <!-- Display progress_photo.id end -->

        <!-- Display progress_photo.user_id start -->
        <td>
          <% if progress_photo.user.present? %>
            <a href="/users/<%= progress_photo.user.id %>">
              <%= progress_photo.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display progress_photo.user_id end -->

        <!-- Display progress_photo.comment start -->
        <td>
          <%= progress_photo.comment %>
        </td>
        <!-- Display progress_photo.comment end -->

        <!-- Display progress_photo.picture start -->
        <td>
          <img src='<%= progress_photo.picture %>' class='img-fluid'>
        </td>
        <!-- Display progress_photo.picture end -->

        <!-- Display progress_photo.created_at start -->
        <td>
          <%= time_ago_in_words(progress_photo.created_at) %> ago
        </td>
        <!-- Display progress_photo.created_at end -->

        <!-- Display progress_photo.updated_at start -->
        <td>
          <%= time_ago_in_words(progress_photo.updated_at) %> ago
        </td>
        <!-- Display progress_photo.updated_at end -->

        <td>
          <a href="/progress_photos/<%= progress_photo.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @progress_photos, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
